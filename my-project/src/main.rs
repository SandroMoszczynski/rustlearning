use std::collections::HashMap as hashio;
///```
/// use std::cmp::Ordering;
/// use std::io;
/// use std::{cmp::Ordering, io};
/// 
/// use std::io;
/// use std::io::Write;
/// use std::io::{self, Write};
/// ```

fn main() {
    let mut map = hashio::new();
    map.insert(1, 2);
}
